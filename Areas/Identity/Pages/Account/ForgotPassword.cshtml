@page
@model ForgotPasswordModel

@{
    ViewData["Title"] = "نسيت كلمة المرور؟";
}

<style>
    /* Shared Card Styling */
    .card {
        border: none;
        border-radius: 15px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }

    .card-header {
        border-top-left-radius: 15px;
        border-top-right-radius: 15px;
        /* Gradient Header for Forgot Password */
        background: linear-gradient(to right, #ff9800, #f57c00);
        color: #fff;
    }

    .form-floating > .form-control:focus ~ label {
        color: #f57c00;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center align-items-center" style="min-height: 80vh;">
        <div class="col-md-8 col-lg-5">
            <div class="card shadow-sm">
                <div class="card-header text-center">
                    <h4 class="mb-0">@ViewData["Title"]</h4>
                </div>
                <div class="card-body">
                    <p class="text-center">Please enter your email to contact</p>
                    <hr />
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Email" class="form-control" autocomplete="username" placeholder="example@email.com" />
                            <label asp-for="Input.Email">Email Address</label>
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-outline-primary w-100 btn-lg">Send</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
